generator client {
  provider = "prisma-client-js"
  output   = "./src/generated/gassma"
}

// ===== Enum =====

enum Role {
  admin     @map("ADMIN")
  user      @map("USER")
  moderator @map("MODERATOR")
}
